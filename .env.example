# BHS Site Environment Configuration Example
# Kopiera denna fil till .env och fyll i dina egna värden

# Email Configuration
CONTACT_EMAIL=info@bhs.se
SENDER_EMAIL=noreply@example.com

# SMTP Configuration (för att skicka via e-postserver)
# Om dessa lämnas tomma används Ethereal.email för testmejl
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASSWORD=your_password_here