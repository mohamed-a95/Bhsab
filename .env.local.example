# BHS Site Local Environment Configuration
# Kopiera denna fil till .env.local för lokal utveckling i VS Code

# Email Configuration
CONTACT_EMAIL=info@bhs.se
SENDER_EMAIL=noreply@bhs.se

# SMTP Configuration (för att skicka via e-postserver)
# Om dessa lämnas tomma används Ethereal.email för testmejl
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASSWORD=your_password_here

# Development
NODE_ENV=development